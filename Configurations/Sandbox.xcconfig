#include "Base.xcconfig"

APP_BUNDLE_IDENTIFIER = $(IDENTITY_BUNDLE_ID_PREFIX).DNSPilot
DNS_PROXY_BUNDLE_IDENTIFIER = $(APP_BUNDLE_IDENTIFIER).DNSPilot-NE
APP_PRODUCT_NAME = DNSPilot
APP_DISPLAY_NAME = DNSPilot Sandbox
DNSPILOT_ENVIRONMENT = sandbox
